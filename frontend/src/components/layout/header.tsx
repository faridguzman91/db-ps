import React from "react";

const Header = () => {
  // const isActive = (pathname: any) => router.pathname === pathname;

  return (
    <>
      <div className="left">Image Gallery</div>
      <div className="right">
        <a>Upload</a>
      </div>
    </>
  );
};

export default Header;
